---
phase: 02-protocolo-y-reglas
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/.claude/skills/playwright-testing/SKILL.md"
autonomous: true
requirements:
  - PROTO-01
  - PROTO-02
  - PROTO-03
  - PROTO-04
  - PROTO-05
  - PROTO-06
  - PROTO-07
  - PROTO-08
  - PROTO-09

must_haves:
  truths:
    - "SKILL.md contiene exactamente 5 pasos numerados (Paso 0 a Paso 5, donde Paso 3 incluye 3bis)"
    - "Paso 0 verifica instalacion con playwright-cli --version y auto-instala si falla"
    - "Paso 1 busca credenciales en .env.local, .env.development, .env en ese orden"
    - "Paso 1 confirma URL del entorno antes de abrir browser"
    - "Paso 2 abre session nombrada con -s=nombre --persistent y toma snapshot"
    - "Paso 3 ejecuta console + network ante error y muestra tabla de diagnostico"
    - "Paso 3bis muestra resumen obligatorio antes de fix"
    - "Paso 4 aplica fix y muestra diff"
    - "Paso 5 re-test desde Paso 2 (flujo completo, no solo el paso fallido)"
  artifacts:
    - path: "~/.claude/skills/playwright-testing/SKILL.md"
      provides: "Seccion Protocolo de diagnostico con 5 pasos completos"
      contains: "Paso 0"
  key_links:
    - from: "~/.claude/skills/playwright-testing/SKILL.md"
      to: "~/.claude/skills/playwright-testing/references/playwright-cli-commands.md"
      via: "referencia explicita en texto"
      pattern: "references/playwright-cli-commands.md"
---

<objective>
Escribir la seccion "Protocolo de diagnostico" del SKILL.md de playwright-testing, reemplazando el TODO existente con el protocolo completo de 5 pasos (Paso 0 a Paso 5), incluyendo manejo de credenciales, sessions nombradas persistentes, tabla de diagnostico de causa raiz, y ciclo de re-test.

Purpose: Sin el protocolo, el skill no tiene instrucciones operativas — Claude no sabe que pasos seguir cuando necesita diagnosticar un problema de frontend.
Output: SKILL.md actualizado con la seccion de protocolo completa (~60-80 lineas de contenido nuevo).
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-protocolo-y-reglas/02-CONTEXT.md
@.planning/phases/02-protocolo-y-reglas/02-RESEARCH.md
@.planning/phases/01-estructura-base/01-02-SUMMARY.md

<interfaces>
<!-- SKILL.md actual — el frontmatter y las secciones existentes que NO se modifican -->
<!-- Solo se reemplaza el contenido del TODO en "## Protocolo de diagnostico" -->

Estado actual del SKILL.md (28 lineas):
- Lineas 1-5: frontmatter (name, description, allowed-tools) — NO TOCAR
- Lineas 7-11: titulo e intro — NO TOCAR
- Lineas 13-15: ## Protocolo de diagnostico + TODO — REEMPLAZAR TODO con contenido
- Lineas 17-19: ## Reglas + TODO — NO TOCAR en este plan (Plan 02 lo hace)
- Lineas 21-23: ## Triggers y Boundaries + TODO — NO TOCAR en este plan (Plan 02 lo hace)
- Lineas 25-28: ## Referencia de comandos — NO TOCAR

Comandos clave de playwright-cli (de references/playwright-cli-commands.md):
- `playwright-cli --version` (verificar instalacion)
- `playwright-cli list` (listar sessions)
- `playwright-cli -s=nombre open URL --persistent` (abrir session)
- `playwright-cli -s=nombre snapshot` (tomar snapshot)
- `playwright-cli -s=nombre console` (ver consola JS)
- `playwright-cli -s=nombre network` (ver trafico de red)
- `playwright-cli -s=nombre click/fill/etc` (interaccion)
- `playwright-cli -s=nombre state-save auth.json` (guardar estado para OAuth)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir protocolo de 5 pasos en SKILL.md usando skill-creator</name>
  <files>~/.claude/skills/playwright-testing/SKILL.md</files>
  <read_first>
    - ~/.claude/skills/playwright-testing/SKILL.md (estado actual con TODOs)
    - ~/.claude/skills/playwright-testing/references/playwright-cli-commands.md (sintaxis de comandos)
    - .planning/phases/02-protocolo-y-reglas/02-CONTEXT.md (decisiones bloqueadas del usuario)
    - .planning/phases/02-protocolo-y-reglas/02-RESEARCH.md (patrones de escritura y pitfalls)
    - ~/.claude/skills/skill-creator/SKILL.md (como usar skill-creator correctamente)
  </read_first>
  <action>
Usar skill-creator para reescribir el SKILL.md reemplazando SOLAMENTE el comentario TODO dentro de "## Protocolo de diagnostico" con el protocolo completo. Las demas secciones (## Reglas, ## Triggers y Boundaries, ## Referencia de comandos) se mantienen EXACTAMENTE como estan. El frontmatter (name, description, allowed-tools) NO se modifica.

El contenido del protocolo debe ser exactamente esto (formato conciso, 2-3 lineas por paso, comandos exactos):

**Paso 0 — Verificar instalacion**
Correr `playwright-cli --version`. Si falla, instalar automaticamente con `npm install -g playwright-cli`. No preguntar al usuario.

**Paso 1 — URL y credenciales**
Confirmar URL del entorno (dev/staging/prod) — NUNCA asumir produccion. Buscar credenciales (patrones: `*_URL`, `*_USER`, `*_PASSWORD`, `*_EMAIL`, `*_TOKEN`, `*_API_KEY`) en este orden:
1. `.env.local`
2. `.env.development`
3. `.env`
Si encuentra credenciales, usarlas directamente sin pedir confirmacion. Si no hay archivos .env, preguntar URL y credenciales al usuario. No adivinar ni usar defaults.
Para OAuth/2FA: pedir al usuario que se loguee manualmente, luego `playwright-cli -s=nombre state-save auth.json`. En sesiones futuras: `playwright-cli -s=nombre state-load auth.json`.

**Paso 2 — Session + snapshot inicial**
Verificar sessions existentes con `playwright-cli list`. Si hay session del proyecto, reusarla. Si no, abrir nueva:
`playwright-cli -s=<nombre-directorio-proyecto> open <URL> --persistent`
Tomar snapshot obligatorio antes de la primera interaccion: `playwright-cli -s=nombre snapshot`.
Despues de este primer snapshot, cada comando playwright-cli devuelve snapshot automaticamente.
IMPORTANTE: el flag `-s=nombre` es obligatorio en TODOS los comandos (click, fill, snapshot, etc.), no solo en open.

**Paso 3 — Diagnostico ante error**
Ejecutar SIEMPRE ambos comandos ante cualquier error:
`playwright-cli -s=nombre console`
`playwright-cli -s=nombre network`

Incluir tabla de causa raiz compacta:

| Sintoma en console/network | Causa probable | Accion |
|----------------------------|----------------|--------|
| HTTP 401 o 403 | Credenciales incorrectas o expiradas | Verificar .env, re-login |
| HTTP 500 | Error en backend | Revisar logs del servidor |
| Error JS en console | Bug en frontend | Identificar componente, revisar stack trace |
| Sin errores en console/network | Bug UI puro | Inspeccionar estado del DOM con snapshot |

**Paso 3bis — Resumen antes de fix**
OBLIGATORIO: antes de tocar codigo, mostrar al usuario:
- Error encontrado
- Causa identificada (de la tabla)
- Accion propuesta
El usuario confirma o redirige. No aplicar fix sin confirmacion.

**Paso 4 — Aplicar fix**
Corregir el codigo fuente. Mostrar diff de los cambios realizados y explicar que se cambio.

**Paso 5 — Re-test completo**
Repetir el flujo COMPLETO desde Paso 2 (snapshot → reproducir → verificar). No solo el paso que fallo.
El ciclo se cierra cuando el flujo pasa sin error.
NO cerrar la session — queda abierta. Cerrar es decision implicita de Claude cuando termina.

Referencia a syntax detallada: "Ver `references/playwright-cli-commands.md` para opciones completas de cada comando."

RESTRICCIONES:
- No duplicar sintaxis detallada de comandos (eso vive en references/)
- Maximo 2-3 comandos de ejemplo por paso
- El SKILL.md completo (incluyendo secciones que no se tocan) debe quedar por debajo de 200 lineas
- Usar skill-creator para la escritura (no edicion manual directa con Edit/Write tool). Dado que el contenido esta completamente especificado, skill-creator se usa en modo draft sin correr evals (las evals son Phase 3)
  </action>
  <verify>
    <automated>grep -c "Paso" ~/.claude/skills/playwright-testing/SKILL.md | grep -E "^[6-9]|^[1-9][0-9]" && grep "playwright-cli --version" ~/.claude/skills/playwright-testing/SKILL.md && grep ".env.local" ~/.claude/skills/playwright-testing/SKILL.md && grep "state-save" ~/.claude/skills/playwright-testing/SKILL.md && grep "console" ~/.claude/skills/playwright-testing/SKILL.md | grep -v "^#" | head -1 && grep "network" ~/.claude/skills/playwright-testing/SKILL.md | grep -v "^#" | head -1 && grep "Causa probable" ~/.claude/skills/playwright-testing/SKILL.md && grep "Re-test" ~/.claude/skills/playwright-testing/SKILL.md && wc -l ~/.claude/skills/playwright-testing/SKILL.md | awk '{if ($1 < 300) print "OK: " $1 " lines"; else print "FAIL: " $1 " lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contiene "Paso 0" (verificar instalacion) — grep "Paso 0" devuelve match
    - SKILL.md contiene "Paso 1" (URL y credenciales) — grep "Paso 1" devuelve match
    - SKILL.md contiene "Paso 2" (session + snapshot) — grep "Paso 2" devuelve match
    - SKILL.md contiene "Paso 3" (diagnostico) — grep "Paso 3" devuelve match
    - SKILL.md contiene "Paso 4" (fix) — grep "Paso 4" devuelve match
    - SKILL.md contiene "Paso 5" (re-test) — grep "Paso 5" devuelve match
    - SKILL.md contiene "playwright-cli --version" (PROTO-01)
    - SKILL.md contiene "npm install -g playwright-cli" (PROTO-01 auto-install)
    - SKILL.md contiene ".env.local" (PROTO-02 lookup chain)
    - SKILL.md contiene ".env.development" (PROTO-02 lookup chain)
    - SKILL.md contiene "-s=" (PROTO-05 sessions nombradas)
    - SKILL.md contiene "--persistent" (PROTO-05 persistent sessions)
    - SKILL.md contiene "playwright-cli list" (verificar sessions existentes)
    - SKILL.md contiene "console" y "network" en contexto de diagnostico (PROTO-06)
    - SKILL.md contiene tabla con "Causa probable" (PROTO-07)
    - SKILL.md contiene "state-save" (manejo OAuth)
    - SKILL.md contiene "Re-test" o "re-test" (PROTO-09)
    - SKILL.md contiene "references/playwright-cli-commands.md" (referencia a syntax)
    - SKILL.md tiene menos de 300 lineas — `wc -l` devuelve numero < 300
    - El frontmatter (name, description, allowed-tools) NO fue modificado — las primeras 5 lineas son identicas al original
    - Las secciones "## Reglas" y "## Triggers y Boundaries" mantienen sus TODOs intactos
    - La seccion "## Referencia de comandos" sigue intacta al final
  </acceptance_criteria>
  <done>El SKILL.md tiene el protocolo completo de 5 pasos (Paso 0 a Paso 5) con manejo de credenciales, sessions nombradas persistentes, tabla de diagnostico, resumen obligatorio pre-fix, y ciclo de re-test. Las demas secciones permanecen intactas. Menos de 300 lineas total.</done>
</task>

</tasks>

<verification>
- `grep -c "Paso" ~/.claude/skills/playwright-testing/SKILL.md` devuelve 6+ (Paso 0, 1, 2, 3, 3bis, 4, 5)
- `wc -l ~/.claude/skills/playwright-testing/SKILL.md` devuelve < 300
- `head -5 ~/.claude/skills/playwright-testing/SKILL.md` muestra frontmatter original intacto
- `grep "TODO" ~/.claude/skills/playwright-testing/SKILL.md` devuelve exactamente 2 matches (Reglas y Triggers, NO protocolo)
</verification>

<success_criteria>
- La seccion "## Protocolo de diagnostico" tiene los 5 pasos completos con comandos exactos y tabla de diagnostico
- Cada paso es conciso (2-3 lineas + comando) como dicta CONTEXT.md
- El SKILL.md mantiene frontmatter y demas secciones intactas
- Total < 300 lineas
</success_criteria>

<output>
After completion, create `.planning/phases/02-protocolo-y-reglas/02-01-SUMMARY.md`
</output>

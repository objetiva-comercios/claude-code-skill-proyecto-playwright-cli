---
phase: 02-protocolo-y-reglas
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - "~/.claude/skills/playwright-testing/SKILL.md"
autonomous: true
requirements:
  - RULE-01
  - RULE-02
  - RULE-03
  - RULE-04
  - RULE-05
  - RULE-06
  - TRIG-01
  - TRIG-02
  - TRIG-03
  - TRIG-04

must_haves:
  truths:
    - "SKILL.md tiene 6 reglas: 4 NUNCA y 2 SIEMPRE, cada una con razon explicita inline"
    - "La regla anti-MCP (@playwright/mcp) esta visualmente destacada y es inequivoca"
    - "Los boundaries listan 4 casos de NO usar con alternativa concreta"
    - "Los triggers cubren bugs frontend, flujos web, y cualquier interaccion con browser"
    - "El SKILL.md completo tiene menos de 300 lineas"
    - "No quedan comentarios TODO en el SKILL.md"
  artifacts:
    - path: "~/.claude/skills/playwright-testing/SKILL.md"
      provides: "Secciones Reglas y Triggers/Boundaries completas"
      contains: "NUNCA"
  key_links:
    - from: "~/.claude/skills/playwright-testing/SKILL.md seccion Reglas"
      to: "~/.claude/skills/playwright-testing/SKILL.md seccion Protocolo"
      via: "Las reglas refuerzan los pasos del protocolo (snapshot antes de interactuar, console+network ante error)"
      pattern: "SIEMPRE.*snapshot|SIEMPRE.*console"
---

<objective>
Completar el SKILL.md de playwright-testing escribiendo las secciones "Reglas" (6 reglas NUNCA/SIEMPRE con razones inline) y "Triggers y Boundaries" (cuando usar y cuando NO usar el skill), reemplazando los 2 TODOs restantes.

Purpose: Sin reglas explicitas, Claude puede caer en anti-patrones (usar curl, MCP, asumir credenciales). Sin boundaries, el skill se activa donde no debe (API testing, unit tests).
Output: SKILL.md completo sin TODOs, con todas las secciones operativas, < 300 lineas.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-protocolo-y-reglas/02-CONTEXT.md
@.planning/phases/02-protocolo-y-reglas/02-RESEARCH.md
@.planning/phases/02-protocolo-y-reglas/02-01-SUMMARY.md

<interfaces>
<!-- Despues de Plan 01, el SKILL.md tendra: -->
<!-- - Frontmatter intacto (NO TOCAR) -->
<!-- - Intro intacta (NO TOCAR) -->
<!-- - ## Protocolo de diagnostico COMPLETO (NO TOCAR — Plan 01 lo escribio) -->
<!-- - ## Reglas + TODO — REEMPLAZAR en este plan -->
<!-- - ## Triggers y Boundaries + TODO — REEMPLAZAR en este plan -->
<!-- - ## Referencia de comandos intacta (NO TOCAR) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escribir reglas NUNCA/SIEMPRE y triggers/boundaries en SKILL.md usando skill-creator</name>
  <files>~/.claude/skills/playwright-testing/SKILL.md</files>
  <read_first>
    - ~/.claude/skills/playwright-testing/SKILL.md (estado actual despues de Plan 01 — verificar que protocolo esta completo y los 2 TODOs restantes estan en su lugar)
    - .planning/phases/02-protocolo-y-reglas/02-CONTEXT.md (decisiones bloqueadas sobre formato y contenido de reglas)
    - .planning/phases/02-protocolo-y-reglas/02-RESEARCH.md (patrones de escritura para reglas y boundaries)
    - ~/.claude/skills/skill-creator/SKILL.md (como usar skill-creator)
  </read_first>
  <action>
Usar skill-creator para reescribir el SKILL.md reemplazando los 2 TODOs restantes (## Reglas y ## Triggers y Boundaries) con contenido completo. El frontmatter, la intro, el protocolo (Paso 0-5), y la seccion de referencia de comandos NO se modifican.

**Seccion "## Reglas"** — Formato: lista con razon inline despues del dash. Una linea por regla.

Las 4 reglas NUNCA:

1. **NUNCA usar curl o wget para interacciones con el browser** — curl no da acceso a la consola JS, al trafico de red ni al estado del DOM; el diagnostico es imposible sin esa informacion.

2. La regla anti-MCP debe estar visualmente destacada (blockquote con negrita):
> **NUNCA usar Playwright MCP server (@playwright/mcp)** — consume 4x mas tokens que playwright-cli, da menos control sobre el flujo, y no aporta nada que playwright-cli no tenga. playwright-cli es la herramienta correcta para todo uso interactivo del navegador.

3. **NUNCA usar @playwright/test ni scripts ad-hoc de Node.js** — playwright-cli resuelve el caso de uso sin el overhead de setup ni scripts que mantener.

4. **NUNCA asumir credenciales sin verificar .env** — credenciales incorrectas producen diagnostico falso (falsos 401, falsos 403). Siempre ejecutar el Paso 1 del protocolo.

Las 2 reglas SIEMPRE:

5. **SIEMPRE tomar snapshot antes de la primera interaccion** — sin snapshot no hay refs disponibles (e1, e2, e3...) y los comandos click/fill no pueden referenciar elementos.

6. **SIEMPRE ejecutar console + network ante cualquier error** — console solo muestra errores JS, network solo muestra HTTP. El diagnostico requiere ambos para identificar la causa raiz.

7. **SIEMPRE confirmar URL del entorno (dev/staging/prod) antes de abrir browser** — testear en produccion por error puede causar dano real a datos o usuarios.

**Seccion "## Triggers y Boundaries"** — Formato: positivo + negativo con alternativa.

Triggers positivos (cuando SI usar):
"Usar este skill para: bugs de frontend, formularios que no funcionan, errores visuales, login roto, flujos de signup/CRUD que fallan, verificacion de que una pagina carga bien, testing de interfaces en el navegador, debugging de rendering o layout, scraping interactivo, automatizacion de flujos de usuario — cualquier interaccion con un navegador web."

Boundaries negativos (cuando NO usar), formato "NO usar para: X (usar Y)":
- API testing sin frontend → usar `curl` o herramienta de HTTP testing
- Unit tests → usar `vitest` o `jest`
- Queries directas a base de datos → usar SQL directo o cliente de DB
- Generacion de scripts Playwright para CI/CD → usar `@playwright/test` (herramienta distinta)

RESTRICCIONES:
- El SKILL.md completo debe quedar por debajo de 300 lineas (verificar con wc -l)
- No quedan comentarios TODO en ninguna parte del archivo
- Usar skill-creator para la escritura (modo draft, sin correr evals — las evals son Phase 3)
- No modificar frontmatter, intro, protocolo ni referencia de comandos
  </action>
  <verify>
    <automated>grep -c "NUNCA" ~/.claude/skills/playwright-testing/SKILL.md | grep -E "^[4-9]" && grep -c "SIEMPRE" ~/.claude/skills/playwright-testing/SKILL.md | grep -E "^[2-9]" && grep "@playwright/mcp" ~/.claude/skills/playwright-testing/SKILL.md && grep "NO usar" ~/.claude/skills/playwright-testing/SKILL.md && grep "curl" ~/.claude/skills/playwright-testing/SKILL.md | head -1 && grep "vitest" ~/.claude/skills/playwright-testing/SKILL.md && grep "TODO" ~/.claude/skills/playwright-testing/SKILL.md; TODO_EXIT=$?; if [ $TODO_EXIT -ne 0 ]; then echo "OK: no TODOs remaining"; else echo "FAIL: TODOs still present"; fi && wc -l ~/.claude/skills/playwright-testing/SKILL.md | awk '{if ($1 < 300) print "OK: " $1 " lines"; else print "FAIL: " $1 " lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contiene al menos 4 ocurrencias de "NUNCA" — `grep -c "NUNCA"` devuelve 4+
    - SKILL.md contiene al menos 2 ocurrencias de "SIEMPRE" — `grep -c "SIEMPRE"` devuelve 2+
    - SKILL.md contiene "@playwright/mcp" en contexto de regla NUNCA (RULE-02)
    - SKILL.md contiene "curl" en contexto de regla NUNCA (RULE-01)
    - SKILL.md contiene "@playwright/test" en contexto de regla NUNCA (RULE-03)
    - SKILL.md contiene "sin verificar .env" o equivalente (RULE-04)
    - SKILL.md contiene "snapshot antes" o equivalente (RULE-05)
    - SKILL.md contiene "console" y "network" en contexto de SIEMPRE (RULE-06)
    - SKILL.md contiene "NO usar para" con al menos 3 items (TRIG-04)
    - SKILL.md contiene "vitest" o "jest" como alternativa (TRIG-04)
    - SKILL.md contiene "bugs de frontend" o "formularios" en triggers positivos (TRIG-01)
    - SKILL.md contiene "login" o "flujos" en triggers positivos (TRIG-02)
    - SKILL.md contiene "navegador web" o "browser" en triggers positivos (TRIG-03)
    - `grep "TODO" ~/.claude/skills/playwright-testing/SKILL.md` NO devuelve resultados (exit code 1)
    - SKILL.md tiene menos de 300 lineas — `wc -l` devuelve numero < 300
    - El frontmatter (lineas 1-5) es identico al original
    - La seccion "## Protocolo de diagnostico" (escrita en Plan 01) no fue modificada
    - La seccion "## Referencia de comandos" sigue intacta al final
  </acceptance_criteria>
  <done>El SKILL.md tiene las 6 reglas NUNCA/SIEMPRE con razones inline (regla anti-MCP destacada visualmente), la seccion de triggers positivos y boundaries negativos con alternativas, cero TODOs restantes, y el total es menor a 300 lineas. El skill esta completo y operativo.</done>
</task>

</tasks>

<verification>
- `grep -c "NUNCA" ~/.claude/skills/playwright-testing/SKILL.md` devuelve 4+
- `grep -c "SIEMPRE" ~/.claude/skills/playwright-testing/SKILL.md` devuelve 2+
- `grep "TODO" ~/.claude/skills/playwright-testing/SKILL.md` no devuelve resultados
- `wc -l ~/.claude/skills/playwright-testing/SKILL.md` devuelve < 300
- `head -5 ~/.claude/skills/playwright-testing/SKILL.md` muestra frontmatter original intacto
- El SKILL.md completo es un documento coherente sin secciones placeholder
</verification>

<success_criteria>
- Las 6 reglas NUNCA/SIEMPRE estan presentes con razones explicitas inline
- La regla anti-MCP esta visualmente destacada (blockquote o similar)
- Triggers positivos cubren todos los casos de TRIG-01, TRIG-02, TRIG-03
- Boundaries negativos cubren los 4 casos de TRIG-04 con alternativas
- Cero TODOs restantes en todo el archivo
- SKILL.md total < 300 lineas
</success_criteria>

<output>
After completion, create `.planning/phases/02-protocolo-y-reglas/02-02-SUMMARY.md`
</output>

---
phase: 01-estructura-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/playwright-testing/references/playwright-cli-commands.md
  - ~/.claude/skills/playwright-testing/evals/.gitkeep
autonomous: true
requirements:
  - STRUC-02
  - STRUC-03

must_haves:
  truths:
    - "El directorio ~/.claude/skills/playwright-testing/ existe con subdirectorios references/ y evals/"
    - "references/playwright-cli-commands.md contiene el SKILL.md oficial descargado del repo microsoft/playwright-cli"
    - "evals/ tiene un .gitkeep como placeholder para Phase 3"
  artifacts:
    - path: "~/.claude/skills/playwright-testing/references/playwright-cli-commands.md"
      provides: "Referencia oficial de comandos playwright-cli descargada del repo de Microsoft"
      contains: "playwright-cli"
    - path: "~/.claude/skills/playwright-testing/evals/.gitkeep"
      provides: "Placeholder para directorio de evals"
  key_links:
    - from: "~/.claude/skills/playwright-testing/references/playwright-cli-commands.md"
      to: "https://raw.githubusercontent.com/microsoft/playwright-cli/main/skills/playwright-cli/SKILL.md"
      via: "curl download"
      pattern: "name: playwright-cli"
---

<objective>
Crear la estructura de directorios del skill playwright-testing y descargar la referencia oficial de comandos del repo microsoft/playwright-cli.

Purpose: Establecer el scaffolding base que Plan 02 necesita para generar el SKILL.md con skill-creator. La referencia de Microsoft es la fuente autoritativa de sintaxis de comandos.
Output: Directorio ~/.claude/skills/playwright-testing/ con references/playwright-cli-commands.md y evals/.gitkeep
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-estructura-base/01-CONTEXT.md
@.planning/phases/01-estructura-base/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear estructura de directorios y descargar referencia oficial</name>
  <files>
    ~/.claude/skills/playwright-testing/references/playwright-cli-commands.md
    ~/.claude/skills/playwright-testing/evals/.gitkeep
  </files>
  <read_first>
    .planning/phases/01-estructura-base/01-RESEARCH.md
  </read_first>
  <action>
1. Crear la estructura de directorios completa:
```bash
mkdir -p ~/.claude/skills/playwright-testing/references
mkdir -p ~/.claude/skills/playwright-testing/evals
touch ~/.claude/skills/playwright-testing/evals/.gitkeep
```

2. Descargar el SKILL.md oficial del repo microsoft/playwright-cli como referencia de comandos:
```bash
curl -s "https://raw.githubusercontent.com/microsoft/playwright-cli/main/skills/playwright-cli/SKILL.md" \
  -o ~/.claude/skills/playwright-testing/references/playwright-cli-commands.md
```

URL exacta verificada en la research phase. NO usar el repo microsoft/playwright-mcp (ese no tiene SKILL.md).

3. Verificar que el archivo descargado tiene contenido valido (debe contener frontmatter con `name: playwright-cli` y secciones de comandos). Si curl retorna 404 o archivo vacio, reportar error — NO escribir la referencia de memoria.

4. NO crear SKILL.md en este task — eso se hace en Plan 02 con skill-creator.
5. NO crear directorios extra (no examples/, no agents/, no scripts/) — decision explicita del usuario.
  </action>
  <verify>
    <automated>ls -la ~/.claude/skills/playwright-testing/ && ls -la ~/.claude/skills/playwright-testing/references/ && ls -la ~/.claude/skills/playwright-testing/evals/ && head -5 ~/.claude/skills/playwright-testing/references/playwright-cli-commands.md && grep -c "playwright-cli" ~/.claude/skills/playwright-testing/references/playwright-cli-commands.md</automated>
  </verify>
  <acceptance_criteria>
    - `ls ~/.claude/skills/playwright-testing/references/playwright-cli-commands.md` retorna el archivo (existe)
    - `ls ~/.claude/skills/playwright-testing/evals/.gitkeep` retorna el archivo (existe)
    - `head -5 ~/.claude/skills/playwright-testing/references/playwright-cli-commands.md` muestra frontmatter YAML con `name: playwright-cli`
    - `grep -c "playwright-cli" ~/.claude/skills/playwright-testing/references/playwright-cli-commands.md` retorna un numero mayor a 0
    - `ls ~/.claude/skills/playwright-testing/` muestra exactamente: references/ y evals/ (NO SKILL.md todavia, NO otros directorios)
    - El archivo descargado tiene mas de 50 lineas (no es un error 404 o archivo truncado): `wc -l ~/.claude/skills/playwright-testing/references/playwright-cli-commands.md` retorna > 50
  </acceptance_criteria>
  <done>El directorio ~/.claude/skills/playwright-testing/ existe con references/playwright-cli-commands.md descargado del repo oficial de Microsoft y evals/.gitkeep como placeholder</done>
</task>

</tasks>

<verification>
```bash
# Estructura completa
ls -R ~/.claude/skills/playwright-testing/

# Contenido valido de la referencia
head -10 ~/.claude/skills/playwright-testing/references/playwright-cli-commands.md
grep "playwright-cli" ~/.claude/skills/playwright-testing/references/playwright-cli-commands.md | head -5

# No hay SKILL.md todavia (eso es Plan 02)
test ! -f ~/.claude/skills/playwright-testing/SKILL.md && echo "OK: SKILL.md no existe todavia"
```
</verification>

<success_criteria>
- Directorio ~/.claude/skills/playwright-testing/ creado con subdirectorios references/ y evals/
- references/playwright-cli-commands.md contiene el SKILL.md oficial de Microsoft con frontmatter `name: playwright-cli`
- evals/.gitkeep existe como placeholder
- No se creo SKILL.md (eso es responsabilidad de Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-estructura-base/01-01-SUMMARY.md`
</output>

---
phase: 01-estructura-base
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - ~/.claude/skills/playwright-testing/SKILL.md
autonomous: false
requirements:
  - STRUC-01
  - STRUC-04

must_haves:
  truths:
    - "El SKILL.md existe en ~/.claude/skills/playwright-testing/ con frontmatter valido (name + description)"
    - "La description del frontmatter es agresiva/pushy y menciona bugs frontend, formularios, errores visuales, login, CRUD, navegador"
    - "El cuerpo tiene secciones esqueleto con TODOs para Phase 2 (protocolo, reglas, triggers)"
    - "El SKILL.md referencia explicitamente references/playwright-cli-commands.md"
    - "El SKILL.md fue generado usando skill-creator (no escrito manualmente)"
    - "Claude Code reconoce el skill en la lista de skills disponibles"
  artifacts:
    - path: "~/.claude/skills/playwright-testing/SKILL.md"
      provides: "Skill principal con frontmatter y esqueleto para Phase 2"
      contains: "name: playwright-testing"
      min_lines: 15
  key_links:
    - from: "~/.claude/skills/playwright-testing/SKILL.md"
      to: "~/.claude/skills/playwright-testing/references/playwright-cli-commands.md"
      via: "referencia explicita en seccion de comandos"
      pattern: "references/playwright-cli-commands.md"
---

<objective>
Crear el SKILL.md del skill playwright-testing usando skill-creator, con frontmatter agresivo para triggering y secciones esqueleto para Phase 2. Verificar que Claude Code reconoce el skill.

Purpose: Completar la estructura base del skill para que sea visible y activable por Claude Code. El contenido real del protocolo se escribe en Phase 2.
Output: ~/.claude/skills/playwright-testing/SKILL.md funcional y reconocido por Claude Code
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-estructura-base/01-CONTEXT.md
@.planning/phases/01-estructura-base/01-RESEARCH.md
@.planning/phases/01-estructura-base/01-01-SUMMARY.md

<interfaces>
<!-- El Plan 01 crea estos artefactos que este plan consume: -->

Directorio existente tras Plan 01:
```
~/.claude/skills/playwright-testing/
├── references/
│   └── playwright-cli-commands.md   # Referencia oficial de Microsoft
└── evals/
    └── .gitkeep
```

El SKILL.md debe referenciar `references/playwright-cli-commands.md` para la sintaxis de comandos.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generar SKILL.md con skill-creator</name>
  <files>~/.claude/skills/playwright-testing/SKILL.md</files>
  <read_first>
    ~/.claude/skills/skill-creator/SKILL.md
    ~/.claude/skills/defuddle/SKILL.md
    ~/.claude/skills/playwright-testing/references/playwright-cli-commands.md
    .planning/phases/01-estructura-base/01-CONTEXT.md
    .planning/PROJECT.md
  </read_first>
  <action>
Invocar el skill skill-creator para generar el SKILL.md del nuevo skill playwright-testing. El proceso de skill-creator requiere:

1. **Capture Intent** — Proporcionar al skill-creator esta informacion:
   - Nombre: `playwright-testing`
   - Proposito: Forzar a Claude a usar `playwright-cli` como unico punto de entrada al navegador web, con protocolo de diagnostico estructurado
   - Cuando debe activarse: Ante CUALQUIER interaccion con navegador web — bugs de frontend, formularios rotos, errores visuales, verificacion de flujos (login, CRUD, navegacion), testing de interfaces, captura de screenshots, debugging de rendering/layout
   - Output esperado: Diagnostico estructurado usando playwright-cli (snapshot, console, network) y fix verificado
   - El skill aplica a TODOS los stacks (no limitado a React, Vue, etc.)

2. **Write the SKILL.md** — El skill-creator debe generar un SKILL.md con:

   **Frontmatter obligatorio:**
   ```yaml
   ---
   name: playwright-testing
   description: [description agresiva/pushy generada por skill-creator]
   ---
   ```

   La description DEBE ser "pushy" (per skill-creator guidelines y decision del proyecto). Debe mencionar explicitamente: bugs de frontend, formularios que no funcionan, errores visuales, verificacion de flujos web (login, signup, CRUD), testing de interfaces en el navegador, cualquier interaccion con el browser, debugging de rendering y layout, captura de estado de paginas web. Debe incluir una instruccion tipo "Use this skill whenever..." que liste todos estos contextos.

   **Cuerpo con secciones esqueleto:**
   - Seccion introductoria breve: que es playwright-cli y por que usarlo (2-3 lineas)
   - Seccion "Protocolo de diagnostico": marcada con `<!-- TODO: Phase 2 — protocolo de 5 pasos -->` como placeholder
   - Seccion "Reglas": marcada con `<!-- TODO: Phase 2 — reglas NUNCA/SIEMPRE con razones -->`
   - Seccion "Triggers y Boundaries": marcada con `<!-- TODO: Phase 2 — cuando usar y cuando NO usar el skill -->`
   - Seccion "Referencia de comandos": texto que diga `Lee references/playwright-cli-commands.md para la sintaxis completa de comandos playwright-cli (descargado del repo oficial de Microsoft).`

   El SKILL.md completo debe tener MENOS de 300 lineas (constraint del proyecto). Con secciones esqueleto esto deberia ser ~30-60 lineas.

3. **NO crear test cases ni correr evals** — eso es Phase 3. Solo generar el SKILL.md inicial.

4. **NO escribir el protocolo de 5 pasos** — eso es Phase 2. Solo los TODOs como placeholder.

IMPORTANTE: El SKILL.md DEBE ser generado invocando skill-creator, no escrito manualmente. Esto cumple STRUC-04.
  </action>
  <verify>
    <automated>cat ~/.claude/skills/playwright-testing/SKILL.md && echo "---LINE COUNT---" && wc -l ~/.claude/skills/playwright-testing/SKILL.md && echo "---CHECKS---" && grep "name: playwright-testing" ~/.claude/skills/playwright-testing/SKILL.md && grep "description:" ~/.claude/skills/playwright-testing/SKILL.md && grep "references/playwright-cli-commands.md" ~/.claude/skills/playwright-testing/SKILL.md && grep -c "TODO" ~/.claude/skills/playwright-testing/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `~/.claude/skills/playwright-testing/SKILL.md` existe
    - `grep "name: playwright-testing" ~/.claude/skills/playwright-testing/SKILL.md` encuentra match
    - `grep "description:" ~/.claude/skills/playwright-testing/SKILL.md` encuentra match (frontmatter tiene description)
    - La description contiene al menos 3 de estos terminos: "browser", "frontend", "testing", "formulario", "login", "visual", "navegador", "playwright-cli"
    - `grep "references/playwright-cli-commands.md" ~/.claude/skills/playwright-testing/SKILL.md` encuentra match (referencia al archivo de comandos)
    - `grep -c "TODO" ~/.claude/skills/playwright-testing/SKILL.md` retorna al menos 2 (secciones placeholder para Phase 2)
    - `wc -l ~/.claude/skills/playwright-testing/SKILL.md` retorna un numero menor a 300
  </acceptance_criteria>
  <done>SKILL.md generado con skill-creator, con frontmatter valido (name + description pushy), secciones esqueleto con TODOs, y referencia a playwright-cli-commands.md</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar que Claude Code reconoce el skill</name>
  <files>~/.claude/skills/playwright-testing/SKILL.md</files>
  <action>
El usuario debe verificar manualmente que Claude Code reconoce el skill playwright-testing en una nueva sesion. Este paso NO puede automatizarse porque requiere iniciar una nueva instancia de Claude Code y observar su comportamiento de descubrimiento de skills.
  </action>
  <verify>
    <automated>ls ~/.claude/skills/playwright-testing/SKILL.md && grep "name: playwright-testing" ~/.claude/skills/playwright-testing/SKILL.md</automated>
  </verify>
  <what-built>
    El skill playwright-testing fue creado con skill-creator e instalado en ~/.claude/skills/playwright-testing/. Tiene SKILL.md con frontmatter (name + description), secciones esqueleto con TODOs para Phase 2, y referencia a la documentacion oficial de comandos de Microsoft.
  </what-built>
  <how-to-verify>
    1. Abrir una nueva sesion de Claude Code (nueva ventana de terminal)
    2. Preguntar: "What skills are available?" o "Que skills tenes disponibles?"
    3. Verificar que `playwright-testing` aparece en la lista de skills
    4. Verificar que la description mostrada es agresiva/pushy (menciona bugs frontend, navegador, testing, etc.)
    5. Opcionalmente probar: "Tengo un boton que no responde en mi app web" y verificar que Claude menciona o considera usar el skill
  </how-to-verify>
  <acceptance_criteria>
    - Claude Code lista `playwright-testing` entre los skills disponibles
    - La description visible es coherente con el frontmatter del SKILL.md
  </acceptance_criteria>
  <done>Claude Code reconoce el skill playwright-testing en la lista de skills disponibles</done>
  <resume-signal>Escribi "aprobado" si el skill aparece correctamente, o describe que problema encontras</resume-signal>
</task>

</tasks>

<verification>
```bash
# Estructura completa del skill
ls -R ~/.claude/skills/playwright-testing/

# SKILL.md valido
head -20 ~/.claude/skills/playwright-testing/SKILL.md

# Menos de 300 lineas
wc -l ~/.claude/skills/playwright-testing/SKILL.md

# Tiene los elementos clave
grep "name: playwright-testing" ~/.claude/skills/playwright-testing/SKILL.md
grep "description:" ~/.claude/skills/playwright-testing/SKILL.md
grep "references/playwright-cli-commands.md" ~/.claude/skills/playwright-testing/SKILL.md
grep "TODO" ~/.claude/skills/playwright-testing/SKILL.md
```
</verification>

<success_criteria>
- SKILL.md generado con skill-creator (STRUC-04) en ~/.claude/skills/playwright-testing/ (STRUC-03)
- Frontmatter tiene name: playwright-testing y description pushy orientada a triggering (STRUC-01 parcial — el protocolo completo va en Phase 2)
- Cuerpo tiene secciones esqueleto con TODOs para Phase 2
- Referencia explicita a references/playwright-cli-commands.md
- SKILL.md tiene menos de 300 lineas
- Claude Code reconoce el skill (verificacion humana)
</success_criteria>

<output>
After completion, create `.planning/phases/01-estructura-base/01-02-SUMMARY.md`
</output>

---
phase: 03-evals-y-optimizacion
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - ~/.claude/skills/playwright-testing/SKILL.md
autonomous: false
requirements: [EVAL-04]

must_haves:
  truths:
    - "run_loop.py corre sin errores y produce output JSON con best_description y best_score"
    - "La description del frontmatter de SKILL.md fue actualizada con best_description si score >= 70%"
    - "El trigger rate final esta documentado en el SUMMARY"
  artifacts:
    - path: "~/.claude/skills/playwright-testing/SKILL.md"
      provides: "Description optimizada del frontmatter"
      contains: "description:"
  key_links:
    - from: "trigger_evals.json"
      to: "run_loop.py"
      via: "input del script de optimizacion"
      pattern: "eval-set"
    - from: "run_loop.py output"
      to: "SKILL.md frontmatter"
      via: "best_description copiada al campo description:"
      pattern: "description:"
---

<objective>
Correr run_loop.py de skill-creator para optimizar la description del frontmatter de SKILL.md y alcanzar trigger rate >80% (o el mejor resultado tras 5 iteraciones).

Purpose: La description actual fue escrita a mano en Phase 1. run_loop.py la optimiza iterativamente usando las trigger_evals como benchmark, mejorando el trigger rate del skill.
Output: SKILL.md con description optimizada y trigger rate medido.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-evals-y-optimizacion/03-CONTEXT.md
@.planning/phases/03-evals-y-optimizacion/03-RESEARCH.md
@.planning/phases/03-evals-y-optimizacion/03-01-SUMMARY.md

<interfaces>
<!-- run_loop.py output JSON (verificado en RESEARCH.md) -->
```json
{
  "exit_reason": "max_iterations (5)",
  "original_description": "...",
  "best_description": "...",
  "best_score": "7/8",
  "iterations_run": 5
}
```

<!-- SKILL.md frontmatter actual -->
```yaml
---
name: playwright-testing
description: Fuerza el uso de playwright-cli como unico punto de entrada...
allowed-tools: Bash(playwright-cli:*)
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correr run_loop.py y optimizar description</name>
  <files>~/.claude/skills/playwright-testing/SKILL.md</files>
  <read_first>
    - ~/.claude/skills/playwright-testing/SKILL.md (frontmatter actual — la linea description: es lo que se modifica)
    - ~/.claude/skills/playwright-testing/evals/trigger_evals.json (input del script — verificar que existe y tiene 8 entries)
    - .planning/phases/03-evals-y-optimizacion/03-RESEARCH.md (comando exacto de run_loop.py y pitfalls)
  </read_first>
  <action>
Paso 1: Verificar que trigger_evals.json existe y tiene 8 entries (prerequisito).

Paso 2: Correr run_loop.py desde el directorio correcto:
```bash
cd ~/.claude/skills/skill-creator
python -m scripts.run_loop \
  --eval-set ~/.claude/skills/playwright-testing/evals/trigger_evals.json \
  --skill-path ~/.claude/skills/playwright-testing \
  --model claude-sonnet-4-6 \
  --max-iterations 5 \
  --runs-per-query 3 \
  --verbose
```

IMPORTANTE: Correr desde ~/.claude/skills/skill-creator/ (no otro directorio). El script usa python -m que requiere ese cwd.

Paso 3: Analizar el output JSON:
- Si best_score >= 70% (6/8 o mas): Proceder a actualizar SKILL.md
- Si best_score < 70%: NO actualizar SKILL.md. Reportar al usuario el score y las queries que fallaron. Dejar que el usuario decida si revisar los evals o aceptar el resultado.

Paso 4: Si score >= 70%, actualizar SOLO la linea `description:` del frontmatter de SKILL.md con el valor de `best_description` del output JSON. NO modificar ninguna otra linea del archivo (ni el cuerpo, ni name:, ni allowed-tools:).

La actualizacion es un Edit del frontmatter YAML — reemplazar la linea que empieza con `description:` por `description: <best_description>`.

Paso 5: Verificar que SKILL.md sigue siendo parseable (frontmatter YAML valido entre --- delimiters, cuerpo intacto).
  </action>
  <verify>
    <automated>python3 -c "
import sys
path = '/home/sanchez/.claude/skills/playwright-testing/SKILL.md'
with open(path) as f:
    content = f.read()
# Verificar frontmatter valido
assert content.startswith('---'), 'No empieza con ---'
parts = content.split('---', 2)
assert len(parts) >= 3, 'Frontmatter mal formado (falta segundo ---)'
fm = parts[1].strip()
assert 'name: playwright-testing' in fm, 'name: playwright-testing no encontrado'
assert 'description:' in fm, 'description: no encontrado en frontmatter'
assert 'allowed-tools:' in fm, 'allowed-tools: no encontrado'
# Verificar cuerpo intacto
body = parts[2].strip()
assert '## Protocolo de diagnostico' in body, 'Seccion Protocolo falta'
assert '## Reglas' in body, 'Seccion Reglas falta'
assert '## Triggers y Boundaries' in body, 'Seccion Triggers falta'
lines = content.strip().split('\n')
assert len(lines) >= 95, f'SKILL.md demasiado corto ({len(lines)} lineas) — el cuerpo fue modificado?'
print(f'OK: SKILL.md valido, {len(lines)} lineas, frontmatter y cuerpo intactos')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - run_loop.py ejecuto sin errores (exit code 0 o terminacion limpia)
    - El output JSON contiene best_description y best_score
    - Si best_score >= 6/8 (70%+): SKILL.md campo description: fue actualizado con best_description
    - Si best_score < 6/8: SKILL.md NO fue modificado y se reporta al usuario
    - SKILL.md frontmatter contiene name: playwright-testing, description: (nueva o original), allowed-tools: Bash(playwright-cli:*)
    - SKILL.md cuerpo contiene las 3 secciones: Protocolo de diagnostico, Reglas, Triggers y Boundaries
    - SKILL.md tiene al menos 95 lineas (cuerpo no fue truncado ni modificado)
  </acceptance_criteria>
  <done>run_loop.py ejecutado con 5 iteraciones max, description actualizada si score >= 70%, SKILL.md intacto excepto campo description</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar trigger rate y description optimizada</name>
  <files>~/.claude/skills/playwright-testing/SKILL.md</files>
  <action>
Checkpoint de verificacion humana. El usuario revisa el trigger rate final y la nueva description del frontmatter de SKILL.md. No se requiere accion automatizada — Task 1 ya hizo todo el trabajo.

Presentar al usuario:
1. El trigger rate final (best_score del output de run_loop.py)
2. La description original vs la nueva description
3. Confirmacion de que el cuerpo del SKILL.md no fue modificado

El usuario aprueba o pide cambios.
  </action>
  <verify>Aprobacion del usuario via resume-signal</verify>
  <done>Usuario aprobo el trigger rate y la description optimizada</done>
  <what-built>run_loop.py optimizo la description del frontmatter de SKILL.md usando 5 iteraciones con 8 trigger evals (5 positivos + 3 negativos). El trigger rate final y la nueva description estan documentados en el output del script.</what-built>
  <how-to-verify>
    1. Revisar el trigger rate final reportado por Task 1 — debe ser >= 70% (6/8) minimo, idealmente >80% (7/8+)
    2. Leer la nueva description en ~/.claude/skills/playwright-testing/SKILL.md — verificar que sigue cubriendo los contextos de activacion importantes (bugs frontend, formularios, login, errores visuales)
    3. Verificar que el cuerpo del SKILL.md (protocolo, reglas, triggers) NO fue modificado — solo la linea description: del frontmatter cambio
    4. Si el trigger rate es <70%, decidir si: (a) aceptar el resultado, (b) pedir revision de los evals, o (c) correr mas iteraciones
  </how-to-verify>
  <resume-signal>Type "approved" si el trigger rate y la description son aceptables, o describe issues</resume-signal>
</task>

</tasks>

<verification>
- run_loop.py ejecuto completo (5 iteraciones o early exit por score perfecto)
- SKILL.md frontmatter actualizado con best_description (si score >= 70%)
- SKILL.md cuerpo intacto (protocolo, reglas, triggers sin cambios)
- Trigger rate final documentado y verificado por el usuario
</verification>

<success_criteria>
- Trigger rate medido y documentado (objetivo: >80%, minimo aceptable: 70%)
- Description del frontmatter optimizada via run_loop.py (no manual)
- SKILL.md valido con todas las secciones intactas
- Usuario aprueba el resultado
</success_criteria>

<output>
After completion, create `.planning/phases/03-evals-y-optimizacion/03-02-SUMMARY.md`
</output>

---
phase: 03-evals-y-optimizacion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/playwright-testing/evals/evals.json
  - ~/.claude/skills/playwright-testing/evals/trigger_evals.json
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03]

must_haves:
  truths:
    - "evals.json contiene 5 test cases con prompts ambiguos en español que no mencionan playwright"
    - "Cada test case tiene 3-5 assertions relevantes al escenario"
    - "trigger_evals.json contiene 8 entries (5 positivos + 3 negativos)"
    - "Los 3 negativos cubren los 3 boundaries del skill: unit tests, API sin frontend, queries DB"
  artifacts:
    - path: "~/.claude/skills/playwright-testing/evals/evals.json"
      provides: "5 quality evals con formato skill-creator"
      contains: "skill_name"
    - path: "~/.claude/skills/playwright-testing/evals/trigger_evals.json"
      provides: "8 trigger rate evals para run_loop.py"
      contains: "should_trigger"
  key_links:
    - from: "evals.json"
      to: "SKILL.md protocolo"
      via: "expectations verifican que el skill sigue el protocolo de 5 pasos"
      pattern: "playwright-cli"
    - from: "trigger_evals.json"
      to: "run_loop.py"
      via: "formato lista plana que run_loop.py consume directamente"
      pattern: "should_trigger"
---

<objective>
Crear los dos archivos de evaluacion del skill playwright-testing: evals.json (5 quality evals) y trigger_evals.json (8 trigger rate evals).

Purpose: Sin estos archivos no hay forma de medir la calidad del skill ni optimizar la description del frontmatter. Son el input obligatorio para run_loop.py (Plan 02).
Output: Dos archivos JSON validados en ~/.claude/skills/playwright-testing/evals/
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-evals-y-optimizacion/03-CONTEXT.md
@.planning/phases/03-evals-y-optimizacion/03-RESEARCH.md

<interfaces>
<!-- Schema de evals.json (de ~/.claude/skills/skill-creator/references/schemas.md) -->
```json
{
  "skill_name": "example-skill",
  "evals": [
    {
      "id": 1,
      "prompt": "User's example prompt",
      "expected_output": "Description of expected result",
      "expectations": [
        "The output includes X",
        "The skill used script Y"
      ]
    }
  ]
}
```

<!-- Schema de trigger_evals.json (de run_loop.py linea 261) -->
```json
[
  {"query": "...", "should_trigger": true},
  {"query": "...", "should_trigger": false}
]
```

<!-- SKILL.md actual (100 lineas) — las evals verifican este contenido -->
Protocolo: Paso 0 (verificar instalacion) → Paso 1 (URL + credenciales .env) → Paso 2 (session nombrada + snapshot) → Paso 3 (console + network ante error) → Paso 3bis (resumen antes de fix) → Paso 4 (fix + diff) → Paso 5 (re-test completo)
Reglas: NUNCA curl/wget, NUNCA @playwright/mcp, NUNCA @playwright/test, NUNCA asumir credenciales; SIEMPRE snapshot, SIEMPRE console+network, SIEMPRE confirmar URL
Boundaries: NO API testing sin frontend, NO unit tests, NO queries DB, NO scripts CI/CD
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear evals.json con 5 quality evals</name>
  <files>~/.claude/skills/playwright-testing/evals/evals.json</files>
  <read_first>
    - ~/.claude/skills/playwright-testing/SKILL.md (skill completo — las expectations verifican este contenido)
    - ~/.claude/skills/skill-creator/references/schemas.md (schema oficial de evals.json)
    - .planning/phases/03-evals-y-optimizacion/03-CONTEXT.md (decisiones locked sobre prompts y assertions)
  </read_first>
  <action>
Crear ~/.claude/skills/playwright-testing/evals/evals.json con exactamente 5 test cases.

Los 5 escenarios (locked):
1. Error al guardar formulario — prompt tipo: "el formulario de contacto no guarda nada cuando le doy submit, no aparece ningun mensaje de error"
2. Login roto — prompt tipo: "no puedo hacer login en la app, pongo mi usuario y contraseña y me rebota a la misma pagina sin mensaje"
3. CRUD sin credenciales — prompt tipo: "hice un CRUD de productos pero cuando intento crear uno nuevo me da error, no se si es un tema de permisos o que"
4. 403 forbidden — prompt tipo: "me da 403 al entrar a la seccion de admin, antes funcionaba bien"
5. Boton sin respuesta — prompt tipo: "el boton de pagar no hace nada cuando lo aprieto, ni siquiera gira el spinner"

Reglas para los prompts:
- En español, tono "usuario tipico frustrado" (no tecnico)
- NUNCA mencionar "playwright", "testing", "browser", "navegador", "CLI" en los prompts
- Suficiente detalle para que Claude entienda que hay un browser de por medio (2-3 oraciones minimo)

Assertions por caso (3-5 por test, solo las que aplican):
- Nivel 1 (herramienta correcta): "El transcript menciona playwright-cli (no curl, no MCP, no scripts Node.js)"
- Nivel 2 (protocolo): "Usa flag -s= o --session= con nombre de proyecto", "Ejecuta snapshot antes de interactuar", "Ejecuta console Y network ante el error (ambos)", "Muestra resumen de diagnostico antes de tocar codigo"
- Nivel 3 (credenciales): "Busco en archivos .env antes de preguntar al usuario" — SOLO en casos 2, 3, 4 (los que involucran auth)
- Nivel 4 (diagnostico): "Identifica causa raiz segun tabla (401→credenciales, 500→backend, error JS→frontend, sin errores→bug UI puro)"

Estructura JSON exacta:
```json
{
  "skill_name": "playwright-testing",
  "evals": [
    {
      "id": 1,
      "prompt": "...",
      "expected_output": "Claude usa playwright-cli con session nombrada, toma snapshot, ejecuta console + network ante el error, identifica causa raiz y propone fix",
      "expectations": ["...", "...", "..."]
    }
  ]
}
```

NO incluir campo "files" (no aplica a este skill — no hay archivos de input para las evals).
  </action>
  <verify>
    <automated>python3 -c "
import json, sys
with open('/home/sanchez/.claude/skills/playwright-testing/evals/evals.json') as f:
    d = json.load(f)
assert d['skill_name'] == 'playwright-testing', 'skill_name incorrecto'
assert len(d['evals']) == 5, f'Esperados 5 evals, encontrados {len(d[\"evals\"])}'
ids = [e['id'] for e in d['evals']]
assert ids == [1,2,3,4,5], f'IDs incorrectos: {ids}'
for e in d['evals']:
    assert 'prompt' in e and 'expected_output' in e and 'expectations' in e
    assert len(e['expectations']) >= 3, f'Eval {e[\"id\"]} tiene menos de 3 expectations'
    assert len(e['expectations']) <= 5, f'Eval {e[\"id\"]} tiene mas de 5 expectations'
    prompt_lower = e['prompt'].lower()
    for word in ['playwright', 'testing', 'browser', 'navegador', 'cli']:
        assert word not in prompt_lower, f'Eval {e[\"id\"]} prompt contiene palabra prohibida: {word}'
print('OK: 5 evals, 3-5 expectations cada uno, sin palabras prohibidas')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - ~/.claude/skills/playwright-testing/evals/evals.json existe y es JSON valido
    - skill_name es exactamente "playwright-testing"
    - Contiene exactamente 5 evals con ids 1-5
    - Cada eval tiene campos: id, prompt, expected_output, expectations
    - Cada eval tiene entre 3 y 5 expectations
    - Ningun prompt contiene las palabras: playwright, testing, browser, navegador, cli
    - Evals 2, 3, 4 tienen expectation sobre busqueda de credenciales en .env
    - Eval 1 tiene expectation sobre snapshot antes de interactuar
    - Todos los evals tienen expectation sobre playwright-cli como herramienta
  </acceptance_criteria>
  <done>5 quality evals escritos con prompts ambiguos en español y assertions que cubren los 4 niveles de verificacion</done>
</task>

<task type="auto">
  <name>Task 2: Crear trigger_evals.json con 8 trigger rate evals</name>
  <files>~/.claude/skills/playwright-testing/evals/trigger_evals.json</files>
  <read_first>
    - ~/.claude/skills/playwright-testing/SKILL.md (triggers y boundaries — los negativos cubren los boundaries)
    - .planning/phases/03-evals-y-optimizacion/03-CONTEXT.md (decisiones locked sobre los 3 negativos)
    - .planning/phases/03-evals-y-optimizacion/03-RESEARCH.md (formato exacto verificado de run_loop.py)
  </read_first>
  <action>
Crear ~/.claude/skills/playwright-testing/evals/trigger_evals.json como lista JSON plana (NO objeto con skill_name — es formato distinto a evals.json).

5 positivos (should_trigger: true) — mismos escenarios que evals.json pero con variacion en redaccion:
1. Formulario que no guarda — ej: "el formulario no guarda cuando le doy submit, no se que esta pasando"
2. Login roto — ej: "no puedo hacer login, me dice que la contraseña esta mal pero se que es correcta"
3. CRUD sin credenciales — ej: "hice un CRUD de productos pero no puedo crear nada, no se si es permisos o que"
4. 403 forbidden — ej: "me da 403 al entrar a la seccion de admin de la app"
5. Boton sin respuesta — ej: "el boton de pagar no hace nada cuando lo aprieto, ni siquiera gira el spinner"

3 negativos (should_trigger: false) — cubren exactamente los 3 boundaries del skill:
6. Unit tests — "corre los unit tests del proyecto y decime si pasan todos"
7. API sin frontend — "hace un curl a la API de pagos para ver si responde bien"
8. Queries DB — "ejecuta esta query SQL en la base de datos y decime cuantos usuarios hay registrados"

Reglas:
- Formato es lista plana: [{query, should_trigger}, ...]
- NUNCA mencionar "playwright", "testing" (misma regla que evals.json) en los positivos
- Los negativos pueden mencionar herramientas especificas (curl, SQL, unit tests) porque asi habla el usuario
- Prompts con suficiente contexto (2+ oraciones para positivos) para evitar Pitfall 3 del RESEARCH.md
  </action>
  <verify>
    <automated>python3 -c "
import json, sys
with open('/home/sanchez/.claude/skills/playwright-testing/evals/trigger_evals.json') as f:
    d = json.load(f)
assert isinstance(d, list), 'Debe ser lista plana, no objeto'
assert len(d) == 8, f'Esperados 8 entries, encontrados {len(d)}'
positivos = [e for e in d if e['should_trigger'] == True]
negativos = [e for e in d if e['should_trigger'] == False]
assert len(positivos) == 5, f'Esperados 5 positivos, encontrados {len(positivos)}'
assert len(negativos) == 3, f'Esperados 3 negativos, encontrados {len(negativos)}'
for e in d:
    assert 'query' in e and 'should_trigger' in e, f'Entry sin query o should_trigger: {e}'
for e in positivos:
    q = e['query'].lower()
    for word in ['playwright', 'testing']:
        assert word not in q, f'Positivo contiene palabra prohibida: {word}'
print('OK: 8 entries (5 positivos + 3 negativos), formato correcto')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - ~/.claude/skills/playwright-testing/evals/trigger_evals.json existe y es JSON valido
    - Es una lista plana (array top-level), NO un objeto con skill_name
    - Contiene exactamente 8 entries
    - 5 entries tienen should_trigger: true
    - 3 entries tienen should_trigger: false
    - Cada entry tiene exactamente los campos: query, should_trigger
    - Ningun positivo contiene "playwright" o "testing" en el query
    - Los 3 negativos cubren: unit tests, curl/API, query SQL
  </acceptance_criteria>
  <done>8 trigger rate evals (5 positivos + 3 negativos) listos para run_loop.py</done>
</task>

</tasks>

<verification>
- Ambos archivos JSON son parseables sin errores
- evals.json tiene formato {skill_name, evals[...]} (objeto)
- trigger_evals.json tiene formato [...] (lista plana)
- Ningun prompt positivo menciona "playwright" o "testing"
- Los 5 escenarios del ROADMAP estan cubiertos en ambos archivos
</verification>

<success_criteria>
- evals.json: 5 evals con prompts ambiguos y 3-5 expectations cada uno
- trigger_evals.json: 8 entries (5 true + 3 false) en formato lista plana
- Ambos archivos pasan validacion automatizada sin errores
</success_criteria>

<output>
After completion, create `.planning/phases/03-evals-y-optimizacion/03-01-SUMMARY.md`
</output>

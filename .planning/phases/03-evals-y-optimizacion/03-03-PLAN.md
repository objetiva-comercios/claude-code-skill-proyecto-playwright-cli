---
phase: 03-evals-y-optimizacion
plan: 03
type: execute
wave: 1
depends_on: ["03-01", "03-02"]
files_modified:
  - ~/.claude/skills/playwright-testing/evals/evals.json
autonomous: true
gap_closure: true
requirements: [EVAL-03]

must_haves:
  truths:
    - "evals 3 y 4 tienen assertion de session nombrada (-s= o --session=)"
    - "evals 2 y 4 tienen assertion de snapshot antes de interactuar"
  artifacts:
    - path: "~/.claude/skills/playwright-testing/evals/evals.json"
      provides: "5 quality evals con assertions completas"
      contains: "session"
  key_links: []
---

<objective>
Completar las assertions faltantes en evals.json para cobertura completa de SC2.

Purpose: La VERIFICATION.md detectó que evals 3 y 4 carecen de assertion de session nombrada, y evals 2 y 4 carecen de assertion de snapshot. CONTEXT.md dice "solo las assertions que aplican al escenario" — pero session nombrada y snapshot son obligatorios en TODOS los escenarios según el protocolo del SKILL.md (Paso 2 es obligatorio siempre).
Output: evals.json actualizado con assertions completas.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-evals-y-optimizacion/03-VERIFICATION.md
@.planning/phases/03-evals-y-optimizacion/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agregar assertions faltantes de session y snapshot a evals 2, 3 y 4</name>
  <files>~/.claude/skills/playwright-testing/evals/evals.json</files>
  <read_first>
    - ~/.claude/skills/playwright-testing/evals/evals.json (estado actual — 5 evals con assertions parciales)
    - ~/.claude/skills/playwright-testing/SKILL.md (protocolo — Paso 2 es obligatorio siempre: session + snapshot)
  </read_first>
  <action>
Editar evals.json para agregar las assertions faltantes. Cambios específicos:

**Eval 2 (login roto, id=2):** Agregar assertion de snapshot:
- Agregar al array expectations: "Ejecuta snapshot antes de interactuar con el formulario de login"

**Eval 3 (CRUD sin credenciales, id=3):** Ya tiene snapshot assertion. Agregar assertion de session:
- Agregar al array expectations: "Usa flag -s= o --session= con nombre de proyecto en los comandos"

**Eval 4 (403 forbidden, id=4):** Agregar ambas assertions:
- Agregar al array expectations: "Usa flag -s= o --session= con nombre de proyecto en los comandos"
- Agregar al array expectations: "Ejecuta snapshot antes de navegar a la seccion de admin"

Justificación: El Paso 2 del protocolo SKILL.md es obligatorio en TODOS los escenarios — session nombrada y snapshot inicial no son opcionales. La ausencia de estas assertions en algunos evals era un gap de cobertura, no una decisión intencional.

NO modificar las expectations existentes ni los prompts. Solo agregar las faltantes al final del array de expectations de cada eval afectado.
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('/home/sanchez/.claude/skills/playwright-testing/evals/evals.json') as f:
    data = json.load(f)
evals = {e['id']: e for e in data['evals']}
# Eval 2: debe tener snapshot
assert any('snapshot' in exp.lower() for exp in evals[2]['expectations']), 'Eval 2 missing snapshot assertion'
# Eval 3: debe tener session
assert any('-s=' in exp or '--session=' in exp for exp in evals[3]['expectations']), 'Eval 3 missing session assertion'
# Eval 4: debe tener session Y snapshot
assert any('-s=' in exp or '--session=' in exp for exp in evals[4]['expectations']), 'Eval 4 missing session assertion'
assert any('snapshot' in exp.lower() for exp in evals[4]['expectations']), 'Eval 4 missing snapshot assertion'
print('ALL ASSERTIONS PRESENT')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - grep "-s=" ~/.claude/skills/playwright-testing/evals/evals.json retorna al menos 5 lineas (evals 1,2,3,4,5 tienen session assertion)
    - grep -i "snapshot" ~/.claude/skills/playwright-testing/evals/evals.json retorna al menos 5 lineas (evals 1,2,3,4,5 tienen snapshot assertion)
    - python3 -c con json.load del archivo no lanza error (JSON valido)
    - Los 5 evals mantienen sus prompts originales sin cambios
  </acceptance_criteria>
  <done>Los 5 evals tienen assertions de session nombrada Y snapshot, cubriendo completamente los 4 niveles de assertion de CONTEXT.md para cada caso donde aplican</done>
</task>

</tasks>

<verification>
- evals.json es JSON valido con 5 evals
- Todos los evals tienen assertions de herramienta correcta, session, snapshot, y console+network
- Evals 2, 3, 4 tienen assertion de credenciales .env (casos con auth)
- Los prompts no fueron modificados (siguen sin mencionar playwright)
</verification>

<success_criteria>
- SC2 del ROADMAP pasa completamente: todos los evals cubren los 4 niveles de assertion donde corresponden
- evals.json tiene entre 5-6 expectations por eval (antes tenian 4-5)
</success_criteria>

<output>
After completion, create `.planning/phases/03-evals-y-optimizacion/03-03-SUMMARY.md`
</output>

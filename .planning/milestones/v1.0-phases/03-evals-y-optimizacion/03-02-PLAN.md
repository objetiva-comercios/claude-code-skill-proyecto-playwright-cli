---
phase: 03-evals-y-optimizacion
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - ~/.claude/skills/playwright-testing/SKILL.md
  - .planning/phases/03-evals-y-optimizacion/03-02-SUMMARY.md
autonomous: false
requirements: [EVAL-04]

must_haves:
  truths:
    - "run_loop.py corre sin errores y produce output JSON con best_description y best_score"
    - "La description del frontmatter de SKILL.md fue actualizada con best_description si score >= 70%"
    - "El trigger rate final esta documentado en el SUMMARY"
  artifacts:
    - path: "~/.claude/skills/playwright-testing/SKILL.md"
      provides: "Description optimizada del frontmatter"
      contains: "description:"
    - path: ".planning/phases/03-evals-y-optimizacion/03-02-SUMMARY.md"
      provides: "Documentacion del trigger rate final y resultado de optimizacion"
      contains: "best_score"
  key_links:
    - from: "trigger_evals.json"
      to: "run_loop.py"
      via: "input del script de optimizacion"
      pattern: "eval-set"
    - from: "run_loop.py output"
      to: "SKILL.md frontmatter"
      via: "best_description copiada al campo description:"
      pattern: "description:"
    - from: "run_loop.py results.json"
      to: "03-02-SUMMARY.md"
      via: "best_score y best_description documentados en el SUMMARY"
      pattern: "best_score"
---

<objective>
Correr run_loop.py de skill-creator para optimizar la description del frontmatter de SKILL.md y alcanzar trigger rate >80% (o el mejor resultado tras 5 iteraciones).

Purpose: La description actual fue escrita a mano en Phase 1. run_loop.py la optimiza iterativamente usando las trigger_evals como benchmark, mejorando el trigger rate del skill.
Output: SKILL.md con description optimizada, trigger rate medido, y SUMMARY documentando resultados.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-evals-y-optimizacion/03-CONTEXT.md
@.planning/phases/03-evals-y-optimizacion/03-RESEARCH.md
@.planning/phases/03-evals-y-optimizacion/03-01-SUMMARY.md

<interfaces>
<!-- run_loop.py output JSON (verificado en RESEARCH.md) -->
```json
{
  "exit_reason": "max_iterations (5)",
  "original_description": "...",
  "best_description": "...",
  "best_score": "7/8",
  "iterations_run": 5
}
```

<!-- run_loop.py soporta --results-dir para persistir output -->
<!-- Uso: --results-dir /tmp/run_loop_results guarda results.json en subdirectorio con timestamp -->

<!-- SKILL.md frontmatter actual -->
```yaml
---
name: playwright-testing
description: Fuerza el uso de playwright-cli como unico punto de entrada...
allowed-tools: Bash(playwright-cli:*)
---
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correr run_loop.py y optimizar description</name>
  <files>~/.claude/skills/playwright-testing/SKILL.md</files>
  <read_first>
    - ~/.claude/skills/playwright-testing/SKILL.md (frontmatter actual — la linea description: es lo que se modifica)
    - ~/.claude/skills/playwright-testing/evals/trigger_evals.json (input del script — verificar que existe y tiene 8 entries)
    - .planning/phases/03-evals-y-optimizacion/03-RESEARCH.md (comando exacto de run_loop.py y pitfalls)
  </read_first>
  <action>
Paso 1: Verificar que trigger_evals.json existe y tiene 8 entries (prerequisito).

Paso 2: Guardar la description original de SKILL.md en /tmp/original_description.txt para comparacion posterior:
```bash
grep '^description:' ~/.claude/skills/playwright-testing/SKILL.md > /tmp/original_description.txt
```

Paso 3: Correr run_loop.py desde el directorio correcto, usando --results-dir para persistir output y redirigiendo stdout para captura:
```bash
cd ~/.claude/skills/skill-creator
python -m scripts.run_loop \
  --eval-set ~/.claude/skills/playwright-testing/evals/trigger_evals.json \
  --skill-path ~/.claude/skills/playwright-testing \
  --model claude-sonnet-4-6 \
  --max-iterations 5 \
  --runs-per-query 3 \
  --results-dir /tmp/run_loop_results \
  --report none \
  --verbose 2>&1 | tee /tmp/run_loop_output.txt
```

IMPORTANTE: Correr desde ~/.claude/skills/skill-creator/ (no otro directorio). El script usa python -m que requiere ese cwd.

IMPORTANTE: --results-dir /tmp/run_loop_results guarda results.json en un subdirectorio con timestamp. --report none evita abrir navegador. El stdout (JSON) se captura en /tmp/run_loop_output.txt para recovery y verificacion.

Paso 4: Localizar el results.json generado:
```bash
RESULTS_JSON=$(ls -t /tmp/run_loop_results/*/results.json 2>/dev/null | head -1)
```

Paso 5: Analizar el output JSON (desde results.json o stdout):
- Si best_score >= 70% (6/8 o mas): Proceder a actualizar SKILL.md
- Si best_score < 70%: NO actualizar SKILL.md. Reportar al usuario el score y las queries que fallaron. Dejar que el usuario decida si revisar los evals o aceptar el resultado.

Paso 6: Si score >= 70%, actualizar SOLO la linea `description:` del frontmatter de SKILL.md con el valor de `best_description` del output JSON. NO modificar ninguna otra linea del archivo (ni el cuerpo, ni name:, ni allowed-tools:).

La actualizacion es un Edit del frontmatter YAML — reemplazar la linea que empieza con `description:` por `description: <best_description>`.

Paso 7: Verificar que SKILL.md sigue siendo parseable (frontmatter YAML valido entre --- delimiters, cuerpo intacto).
  </action>
  <verify>
    <automated>python3 -c "
import json, sys, glob, os

# 1. Verificar que results.json existe y contiene best_score y best_description
results_files = sorted(glob.glob('/tmp/run_loop_results/*/results.json'), key=os.path.getmtime, reverse=True)
assert len(results_files) > 0, 'No se encontro results.json en /tmp/run_loop_results/ — run_loop.py no produjo output'
results_path = results_files[0]
with open(results_path) as f:
    results = json.load(f)
assert 'best_score' in results, f'results.json no contiene best_score: {list(results.keys())}'
assert 'best_description' in results, f'results.json no contiene best_description: {list(results.keys())}'
assert 'original_description' in results, f'results.json no contiene original_description'
print(f'results.json OK: best_score={results[\"best_score\"]}, iterations={results.get(\"iterations_run\", \"?\")}')

# 2. Parsear score
score_parts = results['best_score'].split('/')
passed = int(score_parts[0])
total = int(score_parts[1])
score_pct = passed / total * 100

# 3. Verificar SKILL.md segun score
path = '/home/sanchez/.claude/skills/playwright-testing/SKILL.md'
with open(path) as f:
    content = f.read()
assert content.startswith('---'), 'SKILL.md no empieza con ---'
parts = content.split('---', 2)
assert len(parts) >= 3, 'Frontmatter mal formado (falta segundo ---)'
fm = parts[1].strip()
assert 'name: playwright-testing' in fm, 'name: playwright-testing no encontrado'
assert 'description:' in fm, 'description: no encontrado en frontmatter'
assert 'allowed-tools:' in fm, 'allowed-tools: no encontrado'
body = parts[2].strip()
assert '## Protocolo de diagnostico' in body, 'Seccion Protocolo falta'
assert '## Reglas' in body, 'Seccion Reglas falta'
assert '## Triggers y Boundaries' in body, 'Seccion Triggers falta'
lines = content.strip().split('\n')
assert len(lines) >= 95, f'SKILL.md demasiado corto ({len(lines)} lineas) — el cuerpo fue modificado?'

# 4. Si score >= 70%, verificar que la description cambio
if score_pct >= 70:
    orig_desc = results['original_description']
    # Extraer description actual del frontmatter
    for line in fm.split('\n'):
        if line.startswith('description:'):
            current_desc = line[len('description:'):].strip()
            break
    # La description deberia haber cambiado (best_description != original)
    if results['best_description'] != orig_desc:
        assert current_desc != orig_desc.strip(), f'Score >= 70% y best_description difiere del original, pero SKILL.md description no fue actualizada'
    print(f'Description actualizada correctamente (score {score_pct:.0f}% >= 70%)')
else:
    print(f'Score {score_pct:.0f}% < 70% — SKILL.md no debio ser modificado (correcto)')

print(f'OK: SKILL.md valido, {len(lines)} lineas, frontmatter y cuerpo intactos, results.json persistido')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - run_loop.py ejecuto sin errores (exit code 0 o terminacion limpia)
    - /tmp/run_loop_results/*/results.json existe y contiene best_description, best_score, original_description
    - Si best_score >= 6/8 (70%+): SKILL.md campo description: fue actualizado con best_description
    - Si best_score < 6/8: SKILL.md NO fue modificado y se reporta al usuario
    - SKILL.md frontmatter contiene name: playwright-testing, description: (nueva o original), allowed-tools: Bash(playwright-cli:*)
    - SKILL.md cuerpo contiene las 3 secciones: Protocolo de diagnostico, Reglas, Triggers y Boundaries
    - SKILL.md tiene al menos 95 lineas (cuerpo no fue truncado ni modificado)
  </acceptance_criteria>
  <done>run_loop.py ejecutado con 5 iteraciones max, output persistido en /tmp/run_loop_results/*/results.json, description actualizada si score >= 70%, SKILL.md intacto excepto campo description</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar trigger rate y description optimizada</name>
  <files>~/.claude/skills/playwright-testing/SKILL.md</files>
  <action>
Checkpoint de verificacion humana. El usuario revisa el trigger rate final y la nueva description del frontmatter de SKILL.md. No se requiere accion automatizada — Task 1 ya hizo todo el trabajo.

Presentar al usuario:
1. El trigger rate final (best_score de /tmp/run_loop_results/*/results.json)
2. La description original vs la nueva description (ambas en results.json)
3. Confirmacion de que el cuerpo del SKILL.md no fue modificado

El usuario aprueba o pide cambios.
  </action>
  <verify>Aprobacion del usuario via resume-signal</verify>
  <acceptance_criteria>
    - El usuario reviso el trigger rate y la description
    - El usuario aprobo el resultado O indico cambios necesarios
  </acceptance_criteria>
  <done>Usuario aprobo el trigger rate y la description optimizada</done>
  <what-built>run_loop.py optimizo la description del frontmatter de SKILL.md usando 5 iteraciones con 8 trigger evals (5 positivos + 3 negativos). El trigger rate final y la nueva description estan en /tmp/run_loop_results/*/results.json.</what-built>
  <how-to-verify>
    1. Revisar el trigger rate final reportado por Task 1 — debe ser >= 70% (6/8) minimo, idealmente >80% (7/8+)
    2. Leer la nueva description en ~/.claude/skills/playwright-testing/SKILL.md — verificar que sigue cubriendo los contextos de activacion importantes (bugs frontend, formularios, login, errores visuales)
    3. Verificar que el cuerpo del SKILL.md (protocolo, reglas, triggers) NO fue modificado — solo la linea description: del frontmatter cambio
    4. Si el trigger rate es <70%, decidir si: (a) aceptar el resultado, (b) pedir revision de los evals, o (c) correr mas iteraciones
  </how-to-verify>
  <resume-signal>Type "approved" si el trigger rate y la description son aceptables, o describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Crear 03-02-SUMMARY.md con resultados documentados</name>
  <files>.planning/phases/03-evals-y-optimizacion/03-02-SUMMARY.md</files>
  <action>
Crear el SUMMARY de este plan documentando los resultados de la optimizacion. Leer /tmp/run_loop_results/*/results.json (el mas reciente) para extraer los datos.

El SUMMARY debe incluir:
1. best_score del output de run_loop.py (el trigger rate final)
2. original_description vs best_description (antes y despues)
3. Confirmacion de si SKILL.md fue actualizado o no (y por que)
4. iterations_run y exit_reason
5. Cualquier observacion del checkpoint del usuario (Task 2)

Usar el template de SUMMARY del framework GSD (@/home/sanchez/.claude/get-shit-done/templates/summary.md).

Escribir en .planning/phases/03-evals-y-optimizacion/03-02-SUMMARY.md
  </action>
  <verify>
    <automated>python3 -c "
import os
path = '/home/sanchez/proyectos/claude-code-skill-proyecto-playwright-cli/.planning/phases/03-evals-y-optimizacion/03-02-SUMMARY.md'
assert os.path.exists(path), '03-02-SUMMARY.md no existe'
with open(path) as f:
    content = f.read()
assert len(content) > 100, f'SUMMARY demasiado corto ({len(content)} chars)'
assert 'best_score' in content.lower() or 'trigger rate' in content.lower(), 'SUMMARY no documenta el trigger rate / best_score'
assert 'description' in content.lower(), 'SUMMARY no documenta la description'
print(f'OK: 03-02-SUMMARY.md existe ({len(content)} chars), contiene trigger rate y description')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - 03-02-SUMMARY.md existe en .planning/phases/03-evals-y-optimizacion/
    - Contiene el best_score (trigger rate final) de run_loop.py
    - Contiene la description original y la nueva (o indica que no cambio)
    - Contiene confirmacion de si SKILL.md fue actualizado
  </acceptance_criteria>
  <done>03-02-SUMMARY.md creado con trigger rate final, description original vs nueva, y estado de actualizacion de SKILL.md documentados</done>
</task>

</tasks>

<verification>
- run_loop.py ejecuto completo (5 iteraciones o early exit por score perfecto)
- results.json persistido en /tmp/run_loop_results/ con best_score y best_description
- SKILL.md frontmatter actualizado con best_description (si score >= 70%)
- SKILL.md cuerpo intacto (protocolo, reglas, triggers sin cambios)
- Trigger rate final documentado en 03-02-SUMMARY.md y verificado por el usuario
</verification>

<success_criteria>
- Trigger rate medido y documentado (objetivo: >80%, minimo aceptable: 70%)
- Description del frontmatter optimizada via run_loop.py (no manual)
- SKILL.md valido con todas las secciones intactas
- 03-02-SUMMARY.md creado con resultados completos
- Usuario aprueba el resultado
</success_criteria>

<output>
After completion, create `.planning/phases/03-evals-y-optimizacion/03-02-SUMMARY.md` (handled by Task 3)
</output>

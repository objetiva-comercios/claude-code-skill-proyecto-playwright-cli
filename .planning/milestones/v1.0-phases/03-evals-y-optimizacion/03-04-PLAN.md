---
phase: 03-evals-y-optimizacion
plan: 04
type: execute
wave: 1
depends_on: ["03-02"]
files_modified:
  - ~/.claude/skills/playwright-testing/SKILL.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
gap_closure: true
requirements: [EVAL-04]

must_haves:
  truths:
    - "El usuario decidio el camino a seguir con el trigger rate (aceptar, ajustar evals, o investigar)"
    - "ROADMAP SC3 y SC4 reflejan la realidad del mecanismo de skill-matching"
    - "La decision esta documentada en STATE.md o SUMMARY"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "SC3 y SC4 actualizados segun decision del usuario"
    - path: "~/.claude/skills/playwright-testing/SKILL.md"
      provides: "Description actualizada si el usuario elige opcion B"
  key_links:
    - from: "decision del usuario"
      to: "ROADMAP.md SC3/SC4"
      via: "actualizacion de success criteria"
      pattern: "Success Criteria"
---

<objective>
Resolver los gaps SC3 (grading.json) y SC4 (trigger rate 80%) con decision del usuario.

Purpose: Hay dos gaps que requieren input humano: (1) SC3 menciona "grading.json" pero run_loop.py produce "results.json" — son procesos distintos en skill-creator y (2) SC4 requiere 80% trigger rate pero el mecanismo de skill-matching no responde a prompts casuales (33% tras 10 iteraciones). Ambos necesitan una decision del usuario sobre como proceder.
Output: ROADMAP actualizado, y opcionalmente SKILL.md description actualizada.
</objective>

<execution_context>
@/home/sanchez/.claude/get-shit-done/workflows/execute-plan.md
@/home/sanchez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-evals-y-optimizacion/03-VERIFICATION.md
@.planning/phases/03-evals-y-optimizacion/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decision sobre SC3 (grading.json) y SC4 (trigger rate 80%)</name>
  <decision>Resolver dos gaps de verificacion que requieren input del usuario</decision>
  <context>
**Gap SC3 — grading.json vs results.json:**
En skill-creator hay dos procesos distintos:
- `run_loop.py` + trigger_evals.json → produce `results.json` (trigger rate optimization). YA EJECUTADO, funciona.
- Grader agent + evals.json → produce `grading.json` (quality eval con assertions). NUNCA EJECUTADO.

El ROADMAP SC3 dice: "skill-creator run_loop.py corre sin errores y produce un grading.json con trigger rate medido". Esto mezcla dos conceptos: run_loop.py produce results.json (no grading.json), y grading.json viene del proceso de quality grading (que no mide trigger rate).

**Gap SC4 — 80% trigger rate:**
Tras 10 iteraciones (2 rondas de 5) con run_loop.py, el best_score es 1/3 (33%). El hallazgo documentado en 03-02-SUMMARY indica que el mecanismo de skill-matching de Claude Code no responde a prompts en lenguaje casual de usuario frustrado — los negativos funcionan perfectamente (trigger_rate=0.0) pero los positivos fallan consistentemente (trigger_rate=0.0 tambien).

La description original, la description "pushier", y descriptions en ingles generadas por run_loop.py — ninguna cambio el resultado. El problema no es la description sino el mecanismo de matching.
  </context>
  <options>
    <option id="option-a">
      <name>Aceptar hallazgo y ajustar ROADMAP</name>
      <pros>Refleja la realidad del mecanismo. No gasta mas tiempo en un problema que no es de la description. Documenta el hallazgo como aprendizaje valioso. SC3 se reescribe para decir "results.json" (que es lo que run_loop.py realmente produce). SC4 se ajusta a "trigger rate medido y documentado" en vez de ">80%".</pros>
      <cons>El objetivo original de >80% trigger rate no se cumple. El skill depende de invocacion manual o keywords tecnicas del usuario.</cons>
    </option>
    <option id="option-b">
      <name>Rehacer evals con prompts mas tecnicos y reintentar</name>
      <pros>Podria alcanzar el 80% si los prompts incluyen keywords como "browser", "interfaz web", "pagina". Mantiene el objetivo original.</pros>
      <cons>Los evals dejan de ser realistas (usuario frustrado no usa esas palabras). Pierde el valor de medir trigger rate en condiciones reales. Gasta mas tiempo en un objetivo que puede no tener solucion dentro del mecanismo actual.</cons>
    </option>
    <option id="option-c">
      <name>Usar la best_description en ingles generada por run_loop.py y reintentar</name>
      <pros>La ultima iteracion genero una description en ingles con enfoque en "problems reported in a web interface" — podria mejorar el matching.</pros>
      <cons>Ya se probo en las iteraciones de run_loop.py y no cambio el resultado (33%). Cambia el idioma de la description (inconsistente con el resto del SKILL.md en espanol).</cons>
    </option>
  </options>
  <resume-signal>Selecciona: option-a (aceptar y ajustar), option-b (prompts tecnicos), o option-c (description ingles)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Aplicar decision del usuario a ROADMAP y artefactos</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, ~/.claude/skills/playwright-testing/SKILL.md</files>
  <read_first>
    - .planning/ROADMAP.md (success criteria actuales de Phase 3)
    - .planning/REQUIREMENTS.md (EVAL-04 marcado como [x] Complete incorrectamente)
    - ~/.claude/skills/playwright-testing/SKILL.md (description actual del frontmatter)
    - .planning/phases/03-evals-y-optimizacion/03-02-SUMMARY.md (hallazgo documentado)
  </read_first>
  <action>
Segun la decision del usuario en Task 1:

**Si option-a (aceptar y ajustar):**
1. En ROADMAP.md, actualizar SC3:
   - De: "skill-creator run_loop.py corre sin errores y produce un grading.json con trigger rate medido"
   - A: "skill-creator run_loop.py corre sin errores y produce results.json con trigger rate medido"
2. En ROADMAP.md, actualizar SC4:
   - De: "La descripcion del frontmatter supera el 80% de trigger rate en el benchmark de skill-creator"
   - A: "El trigger rate fue medido con run_loop.py (best_score 1/3, 33%) y el hallazgo documentado: el mecanismo de skill-matching no responde a prompts casuales — la description cubre los casos de uso pero el trigger automatico requiere keywords tecnicas del usuario"
3. En REQUIREMENTS.md, actualizar EVAL-04:
   - De: "[x] **EVAL-04**: Description optimizada via skill-creator run_loop.py con trigger rate >80%"
   - A: "[x] **EVAL-04**: Description evaluada via skill-creator run_loop.py — trigger rate medido (33%), hallazgo documentado: mecanismo de matching requiere keywords tecnicas"
4. SKILL.md: sin cambios (description original se mantiene).

**Si option-b (prompts tecnicos):**
1. No actualizar ROADMAP ni REQUIREMENTS todavia.
2. No tocar SKILL.md.
3. Documentar en SUMMARY que se procedera a rehacer trigger_evals.json con prompts tecnicos en un plan futuro.

**Si option-c (description ingles):**
1. En SKILL.md, reemplazar la linea `description:` del frontmatter con la best_description en ingles de run_loop.py:
   "Use to investigate and diagnose problems reported in a web interface — when the user says something in their app doesn't work as expected in the browser: a button that does nothing, a form that doesn't save, a page that behaves incorrectly, a flow that breaks (login, CRUD, checkout, signup). This skill drives a real browser to reproduce the issue, capture console errors, and inspect network traffic before touching any code. Also use for: verifying a page loads correctly, capturing screenshots, scraping interactive sites, or automating browser flows. Invoke whenever the user is reporting broken frontend behavior or needs to interact with a real browser — even if they don't mention 'playwright' or 'testing'. Does NOT apply to unit tests, SQL queries, or pure backend debugging."
2. No actualizar ROADMAP ni REQUIREMENTS todavia — se reintentara run_loop.py en un plan futuro.
  </action>
  <verify>
    <automated>
# Verificacion depende de la opcion elegida. Para option-a (la mas probable):
grep -c "results.json" .planning/ROADMAP.md && grep -c "trigger rate medido" .planning/ROADMAP.md && grep -c "hallazgo documentado" .planning/REQUIREMENTS.md
    </automated>
  </verify>
  <acceptance_criteria>
    - Si option-a: ROADMAP.md SC3 contiene "results.json" (no "grading.json"), SC4 contiene "hallazgo documentado"
    - Si option-a: REQUIREMENTS.md EVAL-04 contiene "trigger rate medido" y "hallazgo documentado"
    - Si option-b: SUMMARY documenta decision de rehacer evals
    - Si option-c: SKILL.md frontmatter description empieza con "Use to investigate"
    - En todos los casos: el cuerpo del SKILL.md (protocolo, reglas, boundaries) NO fue modificado
  </acceptance_criteria>
  <done>ROADMAP, REQUIREMENTS y/o SKILL.md actualizados segun la decision del usuario, reflejando la realidad del mecanismo de trigger</done>
</task>

</tasks>

<verification>
- La decision del usuario esta documentada
- ROADMAP SC3 y SC4 reflejan la realidad (si option-a)
- REQUIREMENTS.md EVAL-04 refleja el resultado real
- SKILL.md protocolo y reglas intactos
</verification>

<success_criteria>
- El usuario tomo una decision informada sobre el camino a seguir
- Los artefactos de planificacion reflejan la decision
- No hay discrepancia entre lo documentado y lo real
</success_criteria>

<output>
After completion, create `.planning/phases/03-evals-y-optimizacion/03-04-SUMMARY.md`
</output>
